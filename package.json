{
  "name": "fleetview",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based warehouse robot fleet planner: floor plan in, throughput and ROI out.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:bench": "vitest bench",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "lucide": "^1.27.0",
    "motion": "^12.42.2"
  }
}
